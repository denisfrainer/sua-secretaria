[phases.setup]
nixPkgs = ["python3", "python3Packages.pip", "gcc"]

[phases.install]
cmds = ["python3 -m venv /opt/venv && /opt/venv/bin/python3 -m pip install -r requirements.txt"]

[start]
cmd = "/opt/venv/bin/python3 scripts/scrapers/wolf_hunter.py"